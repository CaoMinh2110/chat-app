package com.truevibeup.feature.search.navigation

import androidx.navigation.NavController
import androidx.navigation.NavGraphBuilder

fun NavGraphBuilder.searchGraph(navController: NavController) {
    // SearchScreen is usually a tab in MainScreen, but we can define it here if needed
    // or just export the composable.
}
